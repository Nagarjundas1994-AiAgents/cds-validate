{
  "name": "cds-validate",
  "version": "1.0.0",
  "description": "Zod validation middleware for SAP CAP — auto-validates request data with type coercion and OData-compliant error responses",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "test": "jest --config jest.config.js",
    "test:watch": "jest --watch",
    "lint": "tsc --noEmit",
    "prepublishOnly": "pnpm build"
  },
  "keywords": [
    "sap",
    "cap",
    "cds",
    "validation",
    "zod",
    "odata",
    "middleware"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/YOUR_USERNAME/cds-validate"
  },
  "peerDependencies": {
    "@sap/cds": ">=7.0.0",
    "zod": ">=3.20.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^25.6.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.4.0",
    "zod": "^3.23.0"
  }
}
